---
layout: post
title: Detalles de matematica de punto flotante
author: Jaime Forero-Romero
tags:
redirect_from: '/weeks/'
---

```cpp
#include <stdio.h>

int main(void){
  float a=0.2;
  float b=2.0;
  double c=0.2;
  double d=2.0;

  printf("a (single): %.20f\n", a);
  printf("b (single): %.20f\n", b);
  printf("c (double): %.20f\n", c);
  printf("d (double): %.20f\n", d);

  return 0;
}

```
